//var cradle = require("cradle")
var redis  = require("redis")
var log    = require("logly")
log.name("title")
log.mode("debug")

var nv = process.env
/*var d = new(cradle.Connection)(env.DBHOST || "localhost", 5984, {
                cache: false, 
								auth: {
									username: env.DBUSER || "gkr",
		              password: env.DBPASS || "__"
								}
						}).database(env.DBNAME || "aubongout")
						*/
var r = new redis.createClient(nv.RSPORT || 6379)
    if(nv.REDIS_PASSWORD) r.auth(nv.REDIS_PASSWORD)

//this.d = d
this.r = r
this.l = log
