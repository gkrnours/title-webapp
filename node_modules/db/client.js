//var cradle = require("cradle")
var redis  = require("redis")
var log    = require("logly")
log.name("title")
log.mode("debug")

var nv = process.env
/*var d = new(cradle.Connection)(env.DBHOST || "localhost", 5984, {
                cache: false, 
								auth: {
									username: env.DBUSER || "gkr",
		              password: env.DBPASS || "__"
								}
						}).database(env.DBNAME || "aubongout")
						*/
var r = new redis.createClient(nv.RSPORT || 6379, nv.RSHOST || "127.0.0.1",
					{auth_pass: nv.REDIS_PASSWORD || ""})
console.log([nv.RSHOST, nv.RSPORT, nv.REDIS_PASSWORD, r])

//this.d = d
this.r = r
this.l = log
